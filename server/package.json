{
  "name": "pwa-text-editor-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "server": "nodemon server.js --ignore client"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
